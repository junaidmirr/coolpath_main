# Render deployment is image-backed and bootstrapped manually in the Render
# dashboard. GitHub Actions builds/pushes GHCR, runs Alembic, then triggers the
# Render deploy hook with an exact SHA-tagged imgURL.
#
# Do not use Render source builds or Render-side migration commands for Phase 5.4.
